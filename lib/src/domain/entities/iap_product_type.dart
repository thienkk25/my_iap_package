/// Định nghĩa loại gói mua (In-App Purchase).
enum IapProductType {
  /// Một lần mua và sở hữu vĩnh viễn (ví dụ: Lifetime Pro).
  lifetime,

  /// Mua dạng đăng ký gia hạn trả phí theo kỳ (ví dụ: Monthly Pro).
  subscription,
}
